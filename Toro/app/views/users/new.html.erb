<% if @errors != nil %>
<div class="validation">
  <ul>
  <% @errors.each do |error|%>
    <li><%= error[0] %>
    <% error.delete_at(0) %>
    <% error.each do |e|%>
      <%e.each do |printable|%>
        <%if printable == e.last%>
          <%=printable%>
        <%else%>
          <%=printable%>,
        <%end%>
      <% end %>
    <%end%>
    <br>
    </li>
  <%end%>
  </ul>
</div>
<% end %>
<%= form_for(@user, :url => create_captain_path, :html => {:class => "form-signin"}) do |f| %>

  <%= f.hidden_field :email, :value => "{Time.now.to_i}#{rand(99)}@example.com" %>  

  <div><%= f.label :username %><br />
  <%= f.text_field :username %></div>

  <div><%= f.label :firstname %><br />
  <%= f.text_field :firstname %></div>

  <div><%= f.label :lastname %><br />
  <%= f.text_field :lastname %></div>

  <div><%= f.label :password %><br />
  <%= f.password_field :password %></div>

  <div><%= f.label :password_confirmation %><br />
  <%= f.password_field :password_confirmation %></div>

  <div><%= f.submit "Add captain", class: "btn btn-success btn-large" %></div>
<% end %>