<%= link_to "<<Back", banks_path%>
<br>
<% if @errors != nil %>
<div class="validation">
  <ul>
  <% @errors.each do |error|%>
    <li><%= error[0] %>
    <% error.delete_at(0) %>
    <% error.each do |e|%>
      <%e.each do |printable|%>
        <%if printable == e.last%>
          <%=printable%>
        <%else%>
          <%=printable%>,
        <%end%>
      <% end %>
    <%end%>
    <br>
    </li>
  <%end%>
  </ul>
</div>
<% end %>
<%= form_for(@bank, :url => create_bank_path, :html => {:class => "form-signin"}) do |f| %>

  <div><%= f.label :bankname %><br />
  <%= f.text_field :bankname %></div>

  <div><%= f.submit "Add bank", class: "btn btn-success btn-large" %></div>
<% end %>